"""3D Gen Studio — Python mesh-processing service.

A small FastAPI app that the Node backend (server.js) proxies to for
CPU/GPU-heavy mesh operations implemented in Python (Auto UV, Auto Retopo, ...).
"""
